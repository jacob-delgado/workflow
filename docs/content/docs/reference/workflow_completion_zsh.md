---
title: "workflow completion zsh"
weight: 30
---

<!-- Generated by `task docs:gen` from the command tree. Do not edit. -->

## workflow completion zsh

Generate the autocompletion script for zsh

### Synopsis

Generate the autocompletion script for the zsh shell.

If shell completion is not already enabled in your environment you will need
to enable it.  You can execute the following once:

	echo "autoload -U compinit; compinit" >> ~/.zshrc

To load completions in your current shell session:

	source <(workflow completion zsh)

To load completions for every new session, execute once:

#### Linux:

	workflow completion zsh > "${fpath[1]}/_workflow"

#### macOS:

	workflow completion zsh > $(brew --prefix)/share/zsh/site-functions/_workflow

You will need to start a new shell for this setup to take effect.


```
workflow completion zsh [flags]
```

### Options

```
  -h, --help              help for zsh
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --dry-run      hold back every write to Jira, the forge, the messaging service, git and files, and say what it would have done
      --log string   append a one-line outline of each request (method, path, status, duration) to FILE, for a bug report
```

### SEE ALSO

* [workflow completion](../workflow_completion/)	 - Generate the autocompletion script for the specified shell

